// ESLint flat config (ESLint 9+/10). `next lint` was removed in Next.js 16 and
// `next build` no longer lints, so `npm run lint` runs ESLint directly.
import nextCoreWebVitals from "eslint-config-next/core-web-vitals";
import nextTypescript from "eslint-config-next/typescript";

const config = [
  ...nextCoreWebVitals,
  ...nextTypescript,
  {
    ignores: [".next/**", "node_modules/**", "playwright-report/**", "test-results/**"],
  },
  {
    // eslint-config-next 16 enables the React Compiler lint rules. The
    // scaffolded components use effect-driven loading/hydration patterns that
    // these rules flag; surface them as warnings rather than failing `lint`.
    rules: {
      "react-hooks/set-state-in-effect": "warn",
      "react-hooks/immutability": "warn",
    },
  },
];

export default config;
